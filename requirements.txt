# GUI Framework
PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0

# AI/LLM Integration
anthropic>=0.18.0
openai>=1.12.0
google-genai>=1.0.0
langchain>=0.1.0
langgraph>=0.0.20

# Hugging Face Integration
huggingface_hub>=0.21.0  # For model downloads and HF Inference API
transformers>=4.47.0,<5.0.0  # For loading and running local models (4.47+ required for Gemma 3)
safetensors>=0.4.0  # For efficient model weight loading
sentencepiece>=0.2.0  # Tokenizer for Gemma, Llama, and other models (required on Windows)
protobuf>=4.25.0  # Required for some model tokenizers

# PyTorch - Local LLM Inference
# NOTE: For GPU/CUDA support, install PyTorch separately BEFORE other packages:
#
# 1. First, check your CUDA version with: nvidia-smi (look at top right for "CUDA Version")
#
# 2. Uninstall existing PyTorch (if installed):
#    pip uninstall torch torchvision torchaudio -y
#
# 3. Install PyTorch with CUDA support:
#    Visit https://pytorch.org/get-started/locally/ for the latest commands
#
#    CUDA 13.1 (latest):
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu131
#
#    CUDA 12.4/12.x:
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
#    CUDA 12.1:
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
#    CUDA 11.8:
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 4. Verify CUDA is working:
#    python -c "import torch; print(f'CUDA available: {torch.cuda.is_available()}'); print(f'Device: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else \"N/A\"}')"
#
# NOTE: The default "pip install torch" gets CPU-only version on Windows/Linux!
torch>=2.5.0  # Core deep learning framework (CPU by default - see note above for GPU)
# torchvision>=0.20.0  # Optional: for image models
# torchaudio>=2.5.0  # Optional: for audio models

# Model Optimization (optional - for quantization)
accelerate>=0.27.0  # For efficient model loading and device mapping
# bitsandbytes>=0.42.0  # For 4/8-bit quantization (Linux/WSL only)

# Document Processing
python-docx>=1.1.0
pypandoc>=1.12
ebooklib>=0.18
markdown>=3.5

# Spell Check and Grammar
language-tool-python>=2.8.0
pyspellchecker>=0.7.2
pyenchant>=3.2.2  # Dictionary-based spell checking with suggestions
nltk>=3.8.1  # Natural language toolkit for word corpus and grammar

# NLP and Text Processing
spacy>=3.7.0  # Advanced NLP: POS tagging, dependency parsing, NER
# After installing, download the English model:
#   python -m spacy download en_core_web_sm

# Image Processing & Generation
Pillow>=10.2.0
requests>=2.31.0

# Apple Silicon (MLX) - Local LLM Inference
# mlx and mlx-lm are macOS/Apple Silicon only and cannot be installed on other platforms.
# Install separately on Mac with:
#   pip install mlx mlx-lm
# Or install from this file on Mac only:
#   pip install -r requirements.txt  (will skip if not on Apple Silicon)
#
# mlx>=0.22.0; sys_platform == "darwin"  # MLX core framework (Apple Silicon only)
# mlx-lm>=0.19.0; sys_platform == "darwin"  # MLX language model inference
#
# Recommended install command on Mac:
#   pip install mlx mlx-lm
#
# To run a 27B model with 4-bit quantization (requires 36GB+ RAM):
#   python -c "from mlx_lm import load; m,t = load('mlx-community/gemma-2-27b-it-4bit')"

# Image Generation (Optional)
# For Apple Silicon (M1/M2/M3/M4/M5):
#   pip install mflux  # MLX-based FLUX model for image generation
#   pip install mlx mlx-lm  # MLX framework (if not already installed)
#
# For NVIDIA GPUs:
#   pip install diffusers  # Hugging Face Diffusers for Stable Diffusion, FLUX
#   Requires PyTorch with CUDA support (see PyTorch section above)
#
# For OpenAI DALL-E 3:
#   Uses the openai package (already included above)

# Data Management
pydantic>=2.5.0
pyyaml>=6.0.1

# Utilities
python-dotenv>=1.0.0

# Secure Credential Storage
keyring>=25.0.0  # Cross-platform credential storage (Windows Credential Manager)

# Text-to-Speech
pyttsx3>=2.90  # Offline TTS using system voices (Windows SAPI5, macOS NSSpeech, Linux espeak)
edge-tts>=6.1.0  # High-quality Microsoft Edge neural voices (requires internet)

# Speech-to-Text
SpeechRecognition>=3.10.0  # Microphone speech recognition (Google, Sphinx, etc.)
PyAudio>=0.2.14  # Microphone access for speech recognition